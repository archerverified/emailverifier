# Lead Validator Environment Configuration

# Server settings
PORT=5050
HOST=0.0.0.0

# Validator mode:
# - 'real': Full email verification with DNS/MX/SMTP checks (default)
# - 'mock': Deterministic verification for testing (no network calls)
VALIDATOR_MODE=real

# Upload limits
MAX_UPLOAD_MB=25

# CSV limits
MAX_CSV_ROWS=10000
MAX_LINE_LENGTH=10000

# CORS: comma-separated origins, empty = same-origin only (secure default)
# Example: http://localhost:3000,http://localhost:8080
CORS_ORIGINS=

# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Flask environment
FLASK_ENV=development
